import { Layout } from '@components';
import { Helmet } from 'react-helmet';

const AboutPage = () => {
  return (
    <Layout>
      <Helmet title="About" />
      About
    </Layout>
  );
};

export default AboutPage;
